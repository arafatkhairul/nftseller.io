# Google OAuth সেটআপ গাইড (বাংলা)

## ✅ কি কি যোগ করা হয়েছে

আপনার NFT Marketplace অ্যাপ্লিকেশনে এখন Google দিয়ে লগইন/রেজিস্টার করার সুবিধা যোগ করা হয়েছে!

### যা যা করা হয়েছে:
- ✅ Login পেজে "Continue with Google" বাটন
- ✅ Register পেজে "Sign up with Google" বাটন
- ✅ Google থেকে ইউজার ডাটা নিয়ে অটোমেটিক অ্যাকাউন্ট তৈরি
- ✅ ইউজারের Google avatar সেভ করা
- ✅ Professional Google logo সহ বাটন ডিজাইন

---

## 🔧 এখন কি করতে হবে

### ১. Google Cloud Console এ যান

1. **এই লিংকে যান:** https://console.cloud.google.com/

2. **নতুন প্রজেক্ট তৈরি করুন:**
   - "Select a project" → "New Project" ক্লিক করুন
   - নাম দিন: "NFTSeller"
   - "Create" ক্লিক করুন

3. **OAuth Credentials তৈরি করুন:**
   - "APIs & Services" → "Credentials" এ যান
   - "Create Credentials" → "OAuth client ID" ক্লিক করুন
   - Application type: "Web application" সিলেক্ট করুন

4. **Authorized Redirect URIs যোগ করুন:**
   ```
   http://localhost:8000/auth/google/callback
   ```

5. **Client ID এবং Client Secret কপি করুন**

---

### ২. .env ফাইলে যোগ করুন

আপনার `.env` ফাইল ওপেন করুন এবং এই লাইনগুলো যোগ করুন:

```env
GOOGLE_CLIENT_ID=আপনার-client-id-এখানে.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=আপনার-client-secret-এখানে
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback
```

---

### ৩. Config Clear করুন

টার্মিনালে এই কমান্ড রান করুন:

```bash
php artisan config:clear
```

---

## 🎉 এখন টেস্ট করুন!

1. **ব্রাউজারে যান:**
   ```
   http://localhost:8000/login
   ```

2. **"Continue with Google" বাটনে ক্লিক করুন**

3. **আপনার Google অ্যাকাউন্ট দিয়ে লগইন করুন**

4. **ড্যাশবোর্ডে redirect হবে!** ✨

---

## 📝 গুরুত্বপূর্ণ নোট

- ✅ Google দিয়ে লগইন করলে ইমেইল অটোমেটিক verified হয়ে যাবে
- ✅ নতুন ইউজার হলে অটোমেটিক অ্যাকাউন্ট তৈরি হবে
- ✅ পুরাতন ইউজার হলে Google ID লিংক হবে
- ✅ ইউজারের Google profile picture সেভ হবে

---

## 🚀 Production এ Deploy করার সময়

Production এ deploy করার আগে:

1. **Google Console এ production redirect URI যোগ করুন:**
   ```
   https://yourdomain.com/auth/google/callback
   ```

2. **Production .env আপডেট করুন:**
   ```env
   APP_URL=https://yourdomain.com
   GOOGLE_REDIRECT_URI=https://yourdomain.com/auth/google/callback
   ```

---

## 🎨 কিভাবে কাজ করে

1. ইউজার "Continue with Google" বাটনে ক্লিক করে
2. Google এ redirect হয়
3. ইউজার authorize করে
4. আপনার অ্যাপে ফিরে আসে
5. অটোমেটিক লগইন/রেজিস্টার হয়
6. Dashboard এ redirect হয়

---

## ✨ সব কিছু সেটআপ হয়ে গেছে!

এখন আপনার ইউজাররা Google দিয়ে সহজেই লগইন/রেজিস্টার করতে পারবে।

বিস্তারিত ইংরেজি গাইড দেখুন: `GOOGLE_OAUTH_SETUP.md`
